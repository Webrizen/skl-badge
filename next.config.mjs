/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
        domains: ['images.unsplash.com', 'images.placeholders.dev', 'cdn.sanity.io', 'cdn.worldvectorlogo.com', 'i.pravatar.cc', 'cdn.prod.website-files.com']
    }
};

export default nextConfig;
